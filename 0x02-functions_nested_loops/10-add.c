#include "main.h"

/**
 * add - Entry point
 * @a , @b : input numbers
 * Description: 'print addition of two numbers'
 *
 * Return: Always 0 (Success)
 */

int add(int a, int b)
{
int x, y;
if ((a + b) > -10 && (a + b) < 10)
return ((a + b));
else
{
x = ((a + b) / 10);
y = ((a + b) % 10);
}
return (xy);
}
