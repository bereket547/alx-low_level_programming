#include"main.h"

/**
 * _isupper - tell the letter if it is upper case
 *
 * @c: input character
 *
 * Return: Always zero
 */

int _isupper(int c)
{

if (c >= 65 && c <= 90)
{
return (1);
}
else
return (0);
}
