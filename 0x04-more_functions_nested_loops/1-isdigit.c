#include"main.h"

/**
 * _isdigit - tell the letter if it is digit
 *
 * @c: input character
 *
 * Return: Always zero
 */

int _isdigit(int c)
{

if (c >= 48 && c <= 57)
{
return (1);
}
else
return (0);
}
