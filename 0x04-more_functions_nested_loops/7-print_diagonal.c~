#include"main.h"

/**
 * print_line - print a line
 * @n: length of the line
 * Description: print line and new line
 *
 * Return: no return
 */

void print_line(int n)
{
int a;

if (n == 0 || n < 0)
_putchar('\n');
else
{
for (a = 0 ; a < n ; a++)
{
_putchar('_');
}
_putchar('\n');
}
}
